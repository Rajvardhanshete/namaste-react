## Questions
- what is cdn
- what is crossorigin
- find various attribute of various tag
- how to write image, a tag
-  

fetching react from cdn is not recomended 

# package.json is a configuration for npm
## need
- 
# Bundlers
## build/packages the app for shiped to production
- Web pack
- Vite
- Parcel

# Two types of dependencies 
1) Dev dependencies (-D)
- required for development
2) Normal dependencies
- used in production also

package.json - keep aprox version
package-lock.json - keep exact what version is installed dependencies
(with the help of this two file we can recreate all dependencies)

transitive dependencies : parcel has its own dependencies -> this dependencies also have thier own dependencies -> This dependencies also have thier own dependencies and so on.

# npx 
- executing a package